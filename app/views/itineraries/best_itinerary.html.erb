<div class="position-fixed d-flex flex-column justify-content-between col-12 col-sm-4 z-2 glass-card-background pe-4" style="height: -webkit-fill-available; padding: 1em 0px;">
  <div class="py-5">
    <div class="liquid-glass p-3">
      <%= render "shared/address", itinerary_objective: @itinerary_objective %>
    </div>
  </div>

  <div class="bottom-sheet z-2 p-3">
    <!-- Header avec icône et titre -->
    <div class="sheet-header">
      <%= image_tag "flaneur.png", alt: "Flâneur", class: "flaneur-icon" %>
      <h2 class="sheet-title">
        Your itinerary: <span id="duration"></span> - <span id="distance"></span>
      </h2>
    </div>

    <!-- Contenu -->
    <div>
      <p class="sheet-text">
        <%= @itinerary.description %>
      </p>
    </div>

    <!-- Boutons -->
    <div class="button-container">
      <div class="pt-1 mb-1">
        <%= link_to "Go", itinerary_objective_itinerary_path(@itinerary_objective, @itinerary), class: "go btn w-100 mb-2"%>
      </div>
      <div class="d-flex gap-1">
        <%= link_to "New itinerary", root_path, class: "btn btn-outline-secondary flex-fill"%>
        <%= link_to "See alternatives", alternative_itinerary_itinerary_objective_itineraries_path(@itinerary_objective), class: "btn btn-secondary flex-fill"%>
      </div>
    </div>
  </div>

</div>

<%= render "shared/map", waypoints: @waypoints %>
