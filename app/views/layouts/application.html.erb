<!DOCTYPE html>
<html>
  <head>
    <title>Detours</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=no">
    <meta name="mobile-web-app-capable" content="yes">
    <%= tag.link rel: "manifest", href: webmanifest_path(format: :json) %>
    <%= tag.link rel: "icon", href: image_url("logo.png"), type: "image/png" %>
    <%= tag.link rel: "apple-touch-icon", href: image_url("logo.png") %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-fluid">
      <% if user_signed_in? %>
      <div class="row" data-controller="toggle">
        <nav class="d-none col-9 sidebar pt-3 bg-white h-100 position-absolute z-3" data-toggle-target="hideable" style="transition: transform 0.3s ease-in-out">
          <%= render "shared/sidenav", itineraries: @itineraries %>
        </nav>
        <div class="col-3 my-4">
          <%= render "shared/flashes" %>
          <%= link_to "#", class: "nav-link text-dark",
                    data: {action: "click->toggle#call", toggle_target: "show"} do %>
          <i class="fa-solid fa-bars z-3"></i>
          <% end %>
      <% end %>
          <%= yield %>

        </div>
      </div>
    </div>
  </body>
</html>
