<!DOCTYPE html>
<html>
  <head>
    <title>Detours - Explore your city the smart way</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=no">
    <meta name="mobile-web-app-capable" content="yes">
    <%= tag.link rel: "manifest", href: webmanifest_path(format: :json) %>
     <%# <%= tag.link rel: "icon", href: image_url("logo.png"), type: "image/png" %>
    <%= tag.link rel: "apple-touch-icon", href: image_url("logo.png") %>
    <%= favicon_link_tag %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://api.mapbox.com/mapbox-gl-js/v3.14.0/mapbox-gl.css" rel="stylesheet">
    <script src="https://api.mapbox.com/mapbox-gl-js/v3.14.0/mapbox-gl.js"></script>
    <%# <!– Facebook Open Graph data –> %>
      <meta property='og:title' content='<%= meta_title %>' />
      <meta property='og:type' content='website' />
      <meta property='og:url' content='<%= request.original_url %>' />
      <meta property='og:image' content='<%= meta_image %>' />
      <meta property='og:description' content='<%= meta_description %>' />
      <meta property='og:site_name' content='<%= meta_title %>' />
  </head>

  <body>
    <div class="container-fluid">
      <% if user_signed_in? %>
      <div class="row" data-controller="toggle">
        <%# <nav class="opacity-0 z-n1 col-9 sidebar pt-3 bg-white h-100 position-absolute d-none" data-toggle-target="hideable" style="transition: opacity 0.3s ease-in-out"> %>
          <%# <%= render "shared/sidenav", itineraries: Itinerary.all %>
        </nav>
        <div class="", >
          <%= render "shared/flashes" %>
          <button class="nav-toggle"
                  data-action="click->toggle#call"
                  data-toggle-target="show"
                  aria-label="Toggle menu"
                  type="button">
              <span class="lines"></span>
          </button>
      <% end %>
          <%= yield %>

        </div>
      </div>
    </div>
  </body>
</html>
